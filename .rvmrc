
if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2@richie_rich" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2@richie_rich"
else
  rvm --create  "ruby-1.9.2@richie_rich"
fi
